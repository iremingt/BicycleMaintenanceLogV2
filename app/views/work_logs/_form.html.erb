<%= form_with(model: work_log) do |form| %>
  <% if work_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_log.errors.count, "error") %> prohibited this work_log from being saved:</h2>

      <ul>
        <% work_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.number_field :date %>
  </div>

  <div class="field">
    <%= form.label :hoursUntilServiceDue %>
    <%= form.number_field :hoursUntilServiceDue %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
